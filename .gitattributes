# Normalização de texto
* text=auto

# Sample demo (~5 KB) — commit normal, sem LFS
sample_documents/*.pdf -filter -diff

# Nota: faiss_index/ e documents/ estão no .gitignore.
# Não use mais Git LFS neste repo (HF Spaces rejeita binários no histórico).
